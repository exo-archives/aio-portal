Index: web/eXoResources/src/main/webapp/javascript/eXo/core/Util.js
===================================================================
--- web/eXoResources/src/main/webapp/javascript/eXo/core/Util.js	(revision 64579)
+++ web/eXoResources/src/main/webapp/javascript/eXo/core/Util.js	(working copy)
@@ -137,18 +137,25 @@
   if (hour <= 9) hour = "0" + hour ;
   if (minute <= 9) minute = "0" + minute ;
 
-  var AM_PM = "" ;
-  if (hour < 12) {
-    AM_PM = "AM" ;
-  } else if (hour == 12) {
-    AM_PM = "PM" ;
-  } else {
-    AM_PM = "PM" ;
-    hour = hour - 12 ;
+  var time24hFormatObject = document.getElementById("Time24hFormat");
+  var time24hFormat = false;
+  if(time24hFormatObject != undefined)
+	time24hFormat = (time24hFormatObject.innerHTML == "true") ? true : false;
+  
+  var time = hour + ":" + minute ;
+  if(time24hFormat == false) {
+	  var AM_PM = "" ;
+	  if (hour < 12) {
+	    AM_PM = "AM" ;
+	  } else if (hour == 12) {
+	    AM_PM = "PM" ;
+	  } else {
+	    AM_PM = "PM" ;
+	    hour = hour - 12 ;
+	  }
+	  
+	  time = hour + ":" + minute + " " + AM_PM ;
   }
-  
-  var time = "" ;
-  time += hour + ":" + minute + " " + AM_PM ;
   var digitalClock = document.getElementById("DigitalClock") ;
   digitalClock.innerHTML = time ;
   setTimeout("eXo.core.ExoDateTime.getTime()", 60000) ;
Index: web/portal/src/main/webapp/groovy/portal/webui/workspace/UIExoStart.gtmpl
===================================================================
--- web/portal/src/main/webapp/groovy/portal/webui/workspace/UIExoStart.gtmpl	(revision 64579)
+++ web/portal/src/main/webapp/groovy/portal/webui/workspace/UIExoStart.gtmpl	(working copy)
@@ -4,6 +4,7 @@
   import org.exoplatform.web.application.JavascriptManager;
   import org.exoplatform.portal.config.model.PageNavigation ;
   import org.exoplatform.portal.webui.util.Util;
+  import org.exoplatform.portal.config.GlobalPortalConfigService;
   
   def rcontext = _ctx.getRequestContext() ;
   JavascriptManager jsmanager = rcontext.getJavascriptManager();
@@ -307,6 +308,7 @@
     <div class="ExoStartButton ButtonNormal" onclick="$onclick" title="<%=_ctx.appRes("UIExoStart.tooltip.begin")%>"><span></span></div>
     <div class="VLineSeparator"><span></span></div>
     <div class="DigitalClockContainer">
+      <div style="display:none" id="Time24hFormat"><%=(uicomponent.getApplicationComponent(GlobalPortalConfigService.class).getTime24hFormat()) ? "true" : "false" %></div>
       <div class="DigitalClock" id="DigitalClock"><span></span></div>
     </div>
     <div class="VLineSeparator"><span></span></div>
