Index: webui/core/src/main/java/org/exoplatform/webui/core/UIComponent.java
===================================================================
--- webui/core/src/main/java/org/exoplatform/webui/core/UIComponent.java	(revision 61861)
+++ webui/core/src/main/java/org/exoplatform/webui/core/UIComponent.java	(working copy)
@@ -118,11 +118,14 @@
   public  void setComponentConfig(String componentId, Component config) throws Exception {
     this.config =  config ;
     if(componentId == null || componentId.length() == 0) componentId = config.getId() ;
-    if(componentId == null) {
-      String type = config.getType() ;
-      componentId = type.substring(type.lastIndexOf('.') + 1) ;
+    if(componentId == null) {
+      if(this.id == null) {
+        String type = config.getType() ;
+        setId(type.substring(type.lastIndexOf('.') + 1));
+      }
+    } else {
+      setId(componentId) ;
     }
-    setId(componentId) ;
   }
   
   public  void setComponentConfig(Class<?> clazz, String id) {
Index: webui/core/src/main/java/org/exoplatform/webui/core/UIPortletApplication.java
===================================================================
--- webui/core/src/main/java/org/exoplatform/webui/core/UIPortletApplication.java	(revision 61861)
+++ webui/core/src/main/java/org/exoplatform/webui/core/UIPortletApplication.java	(working copy)
@@ -31,7 +31,9 @@
   static public String HELP_MODE = "HelpMode" ;
   static public String CONFIG_MODE = "ConfigMode" ;
 
-  public UIPortletApplication() throws Exception {}
+  public UIPortletApplication() throws Exception {
+    this.setId(getName() + "-" + String.valueOf(this.hashCode()));
+  }
   
   @Deprecated
   public int getMinWidth() { return minWidth ; }
Index: webui/core/src/main/java/org/exoplatform/webui/core/lifecycle/UIApplicationLifecycle.java
===================================================================
--- webui/core/src/main/java/org/exoplatform/webui/core/lifecycle/UIApplicationLifecycle.java	(revision 61861)
+++ webui/core/src/main/java/org/exoplatform/webui/core/lifecycle/UIApplicationLifecycle.java	(working copy)
@@ -53,7 +53,7 @@
     }
     UIPortletApplication uiApp = (UIPortletApplication) uicomponent;
     context.getWriter().append("<div id=\"").append(uicomponent.getId()).append("\"").
-    append(" class=\"").append(uicomponent.getId()).append("\">");
+    append(" class=\"").append(uicomponent.getName()).append("\">");
     uiApp.renderChildren();
     context.getWriter().append("</div>");
   }
Index: webui/eXo/src/main/java/org/exoplatform/webui/organization/UIPermissionSelector.java
===================================================================
--- webui/eXo/src/main/java/org/exoplatform/webui/organization/UIPermissionSelector.java	(revision 61861)
+++ webui/eXo/src/main/java/org/exoplatform/webui/organization/UIPermissionSelector.java	(working copy)
@@ -43,7 +43,7 @@
   private Permission permission_;
   private boolean isEditable = true;
 	public UIPermissionSelector() throws Exception {
-    super(null, null) ;		
+    super("UIPermissionSelector", null) ;		
     isEditable = true;
     UIFormPopupWindow uiPopup = addChild(UIFormPopupWindow.class, null, "PopupPermissionSelector");
     uiPopup.setWindowSize(540, 0);  
